# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=flyrank

# Connection string for running locally outside Docker
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/flyrank

# Connection string for running inside Docker Compose (uses service name 'db')
DATABASE_URL=postgresql://postgres:postgres@db:5432/flyrank
